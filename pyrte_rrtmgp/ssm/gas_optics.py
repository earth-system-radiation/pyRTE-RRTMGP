# contains the GasOptics class
# Provides the interface (what the user actually calls and what they get back and gets plugged into rest of pyRTE)
# Imports functions from kernel.py and calls them from inside compute() 

